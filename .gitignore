# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# app data (SQLite transcripts) — never commit game history or keys
/data/
*.sqlite3
*.sqlite3-wal
*.sqlite3-shm

# audio sourcing tool: harvested candidates, the built picker, downloaded
# assets and the manifest. `config.json` (what the picker copies out) lives
# here too — small enough to paste anywhere, so nothing is lost by ignoring it.
/audio/

# logs / local
*.log
.env
.DS_Store
