{view}

You are acting as {monster_name} ({monster_id}). Play it to its instincts and intelligence: pack tactics, self-preservation, hatred of the living, whatever fits.

LEGAL ACTIONS (choose exactly one id)
{actions}

RESPONSE_SHAPE: dm_monster_action
Reply with JSON only:
{{"action": "<one id above>", "params": {{}}, "speech": {speech_shape}, "reasoning": "<25 words max>"}}
Fill "params" only with the fields the chosen action says it needs. Declare intent only — the engine rolls and resolves.
{speech_rule}
